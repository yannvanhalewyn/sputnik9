<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>Foobar</title>
  </head>
  <body>
    {{#if flash}}
      <p class="flash {{flash.type}}">{{flash.message}}</p>
    {{/if}}

    {{{body}}}
  </body>
</html>
